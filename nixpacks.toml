[phases.setup]
nixPkgs = ["nodejs-20_x", "nodePackages.npm", "python311"]

[phases.install]
cmds = [
    "npm install",
    "python -m venv /opt/venv",
    "/opt/venv/bin/pip install -r bot/requirements.txt"
]

[start]
cmd = "PATH=/opt/venv/bin:$PATH npm start"
